# Secrets — never commit these
.env

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.pycache

# Virtual environments
venv/
env/
.venv/
.env/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.code-workspace

# Database
*.db
signals.db

# Backtest data cache (local pickles)
backtest_cache/
backtest_cache_hist/

# Backtest scratch output
bt_*.log
*_trades.csv

# Backtest dashboards
*.html
work_*.csv
reports/*.csv
bt_*.csv
funding_history.json

# Backtest trade exports — regenerate with --export-trades, never commit
raw_*.csv
honest_*.csv

# Research data stays on disk: reports/audit_*/ holds candle caches and result
# tables (74 MB and growing). Scripts and write-ups are tracked; the tables they
# produce are not - re-running the script rebuilds them.
*.pkl
*.joblib
*cache*/
reports/**/*.csv
reports/**/*.json
reports/**/*.log
*.npz
bars/
